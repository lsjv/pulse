services:
  - type: web
    name: pulse-frontend
    env: static
    buildCommand: cd pulse_frontend && npm install && node node_modules/vite/bin/vite.js build
    staticPublishPath: pulse_frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html