services:
  - type: web
    name: pulse-frontend
    env: static
    rootDir: pulse_frontend
    buildCommand: npm install && node node_modules/vite/bin/vite.js build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html